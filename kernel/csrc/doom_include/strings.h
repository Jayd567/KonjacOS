#ifndef KONJAC_STRINGS_H
#define KONJAC_STRINGS_H

#include <string.h>

/* strings.h historically just re-exports the case-insensitive compares
   that live in string.h on most modern libcs; doomgeneric's core files
   only ever pull this in for strcasecmp/strncasecmp. */

#endif
