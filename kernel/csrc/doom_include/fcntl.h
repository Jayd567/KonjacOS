#ifndef KONJAC_FCNTL_H
#define KONJAC_FCNTL_H

/* Same story as unistd.h: nothing in the included file set actually uses
   open()/O_* flags, this just needs to exist so #include <fcntl.h>
   doesn't fail to resolve. */

#endif
